# 修改记录表

> 用途：追踪本项目的每次功能开发 / 缺陷修复 / 参数调整。
> 规则：新记录写在最上面；每条记录写清 **动机、改动内容、涉及文件、验证方式、兼容性影响**，
> 方便以后回溯"为什么改、改了什么、怎么证明改对了"。

---

## [2026-07-10] 侧边激光换装右侧（对齐真机）+ 沿墙"零摇摆"加严 + 全链路镜像重训

### 背景 / 动机

1. **侧边传感器装反了**：仿真侧边线激光装在机身【左侧】，真机装在【右侧】。
   沿墙方向因此相反（真机墙在右、绕房间逆时针），机器人建模与整个训练任务
   必须镜像后重训；
2. **沿墙摇摆是硬要求清零项**：新家居建模下回放，`vacuum_wf_0708` 可满足
   demo 但贴墙直行仍有左右摇摆（|yaw_rate|≈0.6rad/s 的极限环）——按要求
   沿墙时**完全不能摇摆**；
3. `vacuum_wf0709` / `vacuum_wf07091` 启动即打转、无法沿墙 —— 四个根因
   （基站架空 / seek 防打转罚饱和 / 丢墙重找零惩罚 / 超速零代价）已在同日
   "家居打转根因修复"条目中定位并改码，但尚未重训生效，本次随镜像一并重训。

### 改动内容

**1) 侧边激光左 → 右（全链路镜像）**

| 文件 | 改动 |
|---|---|
| `envs/vacuum/gen_xml.py` | 侧激光 site 从 (0, +R) 移到 (0, −R)，射向 z→−y；命名 `laser_left_*`/`rf_left_*` → `laser_right_*`/`rf_right_*` |
| `envs/vacuum/sensors.py` | 文档：prefix 'left' → 'right' |
| `envs/vacuum/vacuum_env.py` / `vacuum_wf_env.py` | `laser_left` → `laser_right`；观测中左侧距离/置信/变化率语义变为右侧（**布局与维度不变，仍 34 维**） |
| `tasks/wall_follow_task.py` | 课程整体镜像：随机折线墙摆到机器人**右侧**（起点 y 取负、墙法线取右法线）、head_on 出生 yaw≈−90°、缺口桥接横向判定法线翻转、U 型包边改"连做两个右外角"；`left_read` → `side_read`、`laser_left` → `laser_right` |
| `envs/vacuum/wall_follower.py` | 规则控制器整体镜像：`TURN_RIGHT`→`TURN_LEFT`（内角左转 +1.4rad/s）、`WRAP_LEFT`→`WRAP_RIGHT`（阳角原地右转 −1.4rad/s、偏航积分取负）、FOLLOW PD 输出取负（离墙偏远 → 右转贴墙）、脱困换向改左转；`LEFT_ACQUIRE`→`SIDE_ACQUIRE` |
| `scripts/wall_follow_demo.py` / `debug_wallfollow.py` / `test_line_laser.py` | 引用与统计改右侧；激光测试墙块摆到 −y 侧 |

**2) 沿墙"零摇摆"加严（`tasks/wall_follow_task.py`）**

贴墙直行分支的 wobble 罚启用专用参数：尺度 `TRACK_YAW_SCALE=0.35`（原共用
0.6）、饱和上限 `TRACK_WOB_CAP=2.5`（原 1.0）。0708 版残留的 0.6rad/s 摇摆
在旧参数下每步只罚 0.10×track（相对 track+progress 收益 ~0.54 太弱，策略停
在"摇着走"的极限环）；现同样的摇摆罚 **−0.25×track/步**（决定性劣势），而
直墙跟踪的真实微调（|yaw_rate|≤0.15rad/s）只罚 <0.006/步、不受影响。拐角
不误伤：内角时 front 有回波（门控=0）、外角时侧墙已丢（走包边分支）——
加严只作用于"贴边良好的直行段"。盲区 seek / 缺口桥接分支参数不变（不回归
防打转与桥接行为）。

### 验证

1. **激光特性断言 12/12 全过**（测距 3/6/9cm 准确、12cm 无回波、检测高度
   盲区/低置信/可靠分层正确、右侧激光 2cm 测距准确）；
2. **镜像规则控制器整屋巡边**（家居场景 headless 300s，speed 2.0）：门洞
   穿越 3 次（进出里间贯通）、FOLLOW 稳态 |d−1cm| 中位 2.3mm、±5mm 内 92%，
   与镜像前水平一致；
3. **镜像与奖励数值检查 15 项全过**：40/40 次 reset 首段墙在机器人右侧；
   贴墙回合右侧激光 10/10 命中；贴墙 0.6rad/s 摇摆罚 = −0.25×track、
   0.15rad/s 微调 <0.006、静止为 0；seek 3rad/s 打转罚封顶 −0.24 不回归；
   iter 0/300/800/1500/3000 课程 reset+随机步奖励/观测全有限；家居出生点
   (−0.64, 0.70) 朝 +x、100 随机步无异常；
4. **端到端训练冒烟 2 迭代**：KL 0.005 健康、回报有限、4 进程无 Ray 异常。

### 兼容性影响

- **观测/动作维度不变（34/2）、奖励 dict 键不变**，但侧激光语义已从左镜像
  到右 —— **所有旧模型（0708/0709/07091）对新环境全部作废**，回放行为无意义，
  必须重训；
- 规则控制器 demo 已同步镜像，行为等价（巡边方向变为逆时针）；
- 已启动重训：`experiments/vacuum_wf0710`（2000 迭代、8 进程 —— 系统空闲
  内存仅 3.5GB，沿用 12 进程有被 Ray 杀掉的风险；其余超参与 0708/0709 一致：
  lr 1e-4、max_traj_len 600、target_kl 0.04、lr_decay）。

---

## [2026-07-10] 家居打转根因修复：基站架空 / wobble 饱和无梯度 / 丢墙重找零惩罚 / 超速零代价

### 背景 / 现象

0709 改动重训的两版模型（`vacuum_wf0709` / `vacuum_wf07091`）家居回放仍然
全面失败：出生后在基站处"困住"几秒再暴力甩出；全程打转找不到墙；即使碰到
墙也很快丢墙、继续打转，完全无法沿墙。

### 定量诊断（先量化，再定位）

对两版模型各跑随机墙（满课程）6 回合 + 家居 4 回合，逐步记录激光/动作/
偏航角速度/奖励分支：

- **随机墙上沿墙是成立的**（贴边命中 97~99%、误差 3~8mm）→ 策略没有崩，
  问题在训练分布与家居场景的系统性差异 + 奖励漏洞；
- 家居出生段前 3.5s：策略输出满幅差速力矩（动作跑飞到 ±20），但
  yaw_rate≈0、位移≈0 —— **物理卡死**而非犹豫；
- 0709 在随机墙的 seek 段：|yaw_rate|=2.6~3.0rad/s、spin 占比 ~90% ——
  **训练分布内 seek 就在螺旋打转**，0709 的防打转罚没起作用；
- 家居丢墙后（acquired 且 lost>1.2s）：|yaw_rate| 3~4rad/s 高速打转直到
  丢墙超时终止；
- 沿墙巡航速度 0.24~0.34m/s = **2×V_DES**（progress 在 V_DES 饱和、超速
  零代价），10cm 量程激光反应时间减半，振荡/丢墙全被放大。

### 根因（四个，彼此独立）

1. **基站底板高位架空（昨天出生点改动引入的机械死局）**：底板宽 26cm <
   轮距 30cm，机器人骑在底板上时前/后万向球压板把机身架起、两驱动轮悬空
   失去抓地。受控实验：底板上满力矩前进/原地转 2s 位移与偏航均为 0.000；
   底板前空地上同指令 2s 前进 1.24m / 转 2.08rad。"困在基站再暴力甩出"
   全由此来。
2. **seek 防打转罚在 0.6rad/s 就饱和（`min(x²,1.0)`）**：打转封顶只罚
   -0.06/步，而螺旋推进拿 progress +0.13/步 —— 打转净赚；且饱和区对
   "转多快"零梯度，策略停在 2.5~3rad/s 螺旋没有任何下降方向。
3. **丢墙重找分支（acquired 且 lost>WRAP_GRACE）没有防打转**：steady=0、
   progress×0.2 —— 打转零代价。训练课程丢墙段短、分布内很快重捕获，把
   这个洞掩盖；家居的家具/薄隔断/矮基站处一旦丢墙就掉进这个零梯度区。
4. **progress 是 clip 形，超速零代价**：策略巡航到 2×V_DES，动态裕量被
   吃光；高速也让"螺旋找墙"覆盖面积大、净收益为正，加剧 #2。

### 改动内容

| 文件 | 改动 |
|---|---|
| `tasks/wall_follow_task.py` | ① 家居出生点移到基站底板正前方 (-0.64,0.70)、轮子满抓地，朝 +x ±0.15rad（出生即出发）；② progress 改"帐篷"形：V_DES 拿满、超速线性扣回、2×V_DES 归零、更快为负（fwd≤V_DES 一侧与旧公式完全一致）；③ 丢墙重找并入 seek 同治：progress×0.5 + 防打转（原 ×0.2 + steady=0）；④ 盲区巡航 wobble 饱和上限 1.0→4.0（1.2rad/s 才饱和，打转封顶罚 -0.24/步）；⑤ 全盲出生占比 0.40→0.60、d0 上限 0.60→1.00m（覆盖家居 ~2.4m 长盲巡航）；⑥ 家居回放丢墙宽限 ×2→×4；⑦ V_DES 0.15→0.28（用户要求保持 0.24~0.34 巡航带：帐篷峰值=训练出的巡航速度，带宽 0.24~0.32 内速度分 ≥0.86；代价是 10cm 量程下反应时间 ~0.33s，振荡/拐角冲过量预期比 0.15 版大） |
| `envs/vacuum/vacuum_wf_env.py` | 观测中 prev_action 限幅 ±1（\|a\|>1 后力矩已被 ctrlrange 饱和，不限幅时越界动作经观测正反馈自激，家居实测跑飞到 ±20） |

关键取舍：帐篷速度分只改 fwd>V_DES 一侧，刹车/慢速接近的既有梯度不变；
wobble 上限只在盲区巡航分支放宽，贴墙直行/拐弯/正对墙原地转向的门控与
数值与旧版完全一致，不误伤既有正确行为。

### 验证

1. **奖励分支数值检查 22 项全过**：帐篷四点（V_DES 满分/2×V_DES 归零/
   0.5m/s 封底/半速与旧值一致）；seek 打转封顶 -0.24、直行 vs 螺旋每步
   优势 0.50、轻弧巡航仅 -0.010；丢墙重找打转吃满罚、直行 ×0.5；包边
   窗口/正对墙转向/贴墙直行 wobble/缺口桥接均与旧版一致不回归。
2. **机械修复**：底板上满力矩 2s 位移 0（复现死局）；新出生点前进指令
   1s 位移 0.56m、转向指令 1s 偏航 2.52rad。
3. **旧模型新出生点家居回放**：出生 3s 位移 0.08~0.82m（旧出生点为
   0.00m + 冻结 3.5s）——"出生即出发"成立；中途打转仍在（预期，需重训）。
4. **课程 reset + 随机步冒烟**：iter 0/300/800/1500/3000 均无异常、奖励有限。
5. **端到端训练冒烟 2 迭代**：回报 19.8→19.0、KL 0.007~0.02 健康。
   （首次尝试因系统内存被其它应用占满 95% 被 Ray 杀掉，属环境问题，
   减进程数复跑通过。）
6. **V_DES=0.28 数值复检**：帐篷带宽 0.24/0.28/0.32/0.34 → 速度分
   0.86/1.00/0.86/0.79、2×V_DES 归零；seek 任意打转组合（fwd 0~0.56 ×
   yaw_rate 1~3rad/s）每步劣势均 ≥0.15；3cm 前满速冲（-0.035）仍严格差于
   按限速减速（+0.014），刹车梯度未被高速带冲掉；环境冒烟无异常。

### 兼容性影响

- 观测/动作维度不变（34/2），奖励 dict 键不变，旧模型可加载回放；
- 奖励函数与观测语义（prev_action 限幅）已改，**必须重训**才能获得
  "出生直行找墙、丢墙不打转、贴墙限速"的新行为；
- 已知余项：策略沿墙时 |yaw_rate|≈0.6 的振荡（spin%~30）预计随帐篷限速
  重训缓解，重训后回放若仍明显，再单独调 wobble 的贴墙门控。

---

## [2026-07-10] 家居场景出生点改为回充基站（从基站出发）

> ⚠️ 本条的出生点 (-0.86, 0.70, 0.055)（骑在基站底板上）已被同日
> "家居打转根因修复"条目修正：底板宽 26cm < 轮距 30cm，骑板出生会被
> 万向球架空、驱动轮悬空，满力矩也无法移动（当日验证只查了静置稳定与
> 穿插，漏查了轮子受载）。现出生点为底板正前方 (-0.64, 0.70)。

### 背景 / 动机

按新算法重训后回放，家居场景中机器人仍出生即原地打转、找不到墙。同时按
真机实际使用方式，机器人本就应从回充基站出发开始作业，而不是随机出生在
房间中央。本次把家居仿真场景的出生点固定为基站位置。

### 改动内容

`tasks/wall_follow_task.py` 的 `reset()` 家居分支：原 4 个房间中点随机出生
（`(-0.6,0)/(0.5,0.5)/(1.2,-0.5)/(0,-0.8)` + 随机朝向 ±π）改为固定出生在
回充基站上：

- **位置 (-0.86, 0.70)**：基站座体 `dock` 立面在 x=-1.045（`gen_xml.py`），
  机身（半径 0.175m）后缘距立面 1cm，即真机"停靠在基站上"的位姿；
- **z=0.055**：基站前伸底板 8mm 高、宽 26cm < 轮距 30cm —— 两驱动轮跨在
  底板两侧直接落地，仅后万向球压在底板上；抬高 5mm 出生避免万向球插进
  底板，落地后自稳（实测稳定在 z≈0.054，机身微微后仰，符合真机停靠姿态）；
- **朝向 +x ±0.15rad**：背离基站驶出（基站靠西墙），微扰覆盖真机出站的
  朝向误差。出生后直行约 2.7m 会抵达隔断（x=2.0）北段，从那里开始沿墙。

### 涉及文件

| 文件 | 改动 |
|---|---|
| `tasks/wall_follow_task.py` | `reset()` 家居分支：随机出生点改为基站固定出生 |

### 验证

冒烟测试（5 次 reset）：出生位姿准确（x=-0.860,y=0.700,yaw∈±0.15）；零动作
静置 60 步落座稳定（z 稳定 0.054、漂移 ≤2mm、未触发任何终止条件）；与基站
座体/底板接触穿透 0.0000m（无穿插）；随机动作 100 步无异常。

### 兼容性影响

- 只影响 `home_scene=True` 的回放场景，训练课程（随机墙）完全不受影响，
  **无需重训**，现有模型直接回放即可；
- 观测/动作维度、奖励、XML 均未变；
- 注意：出生点离最近的墙（西墙，背后 ~13cm）在机身后方，激光看不到，
  出生仍属"全盲"状态 —— 能否直行找墙取决于 07-09 的 seek 奖励重训效果，
  本改动只保证出发点与真机一致。

---

## [2026-07-09] 找墙防打转 + 门洞侧柱 U 型包边课程

### 背景 / 现象

家居场景回放暴露两个缺陷：

1. **出生原地转圈**：机器人出生后一直原地旋转，有时转一会才碰运气找到墙，
   有时整个回合都在转。期望：出生后径直巡航找墙，在墙/障碍前 ~1cm 刹停、
   转向对齐、开始沿墙。
2. **门洞侧柱不包边**：沿隔断（x=2.0）走到门洞侧柱（缺口转角）时，机器人
   向外打转甩出，不再沿墙 —— 期望是绕过侧柱拐进里屋（黑砖区）继续沿墙。

### 根因

1. 家居出生点离墙都远超激光量程 10cm（全盲），而奖励里"转圈"与"直行"几乎
   无差：丢墙罚与动作无关、wobble 在丢墙时门控为 0（不罚旋转）、直行只多拿
   progress×0.2 一档弱速度分 —— 转圈是零代价行为。且训练找墙课程 d0 最多
   45cm、偏航 ±0.5，从未见过"任意朝向 + 长时间全盲"的出生分布。
2. 训练课程的段间过渡只有 55°~115° 单拐角和共线缺口桥接，从未出现"绕墙端
   连做两个左外角（≈180°）"的几何 —— 门洞侧柱正是这种。且缺口桥接上限随
   课程升到 0.55m，已逼近家居门洞宽度 0.7m，"桥接跳过"与"外角拐入"边界模糊。

### 改动内容（`tasks/wall_follow_task.py`）

- **seek 找墙巡航分支**：`calc_reward` 中 progress 新增 `not acquired` 分支
  （×0.5，原沦入 ×0.2 档）；wobble 新增 seek 门控 0.6 —— 全盲时旋转对 10cm
  量程激光毫无侦察价值，直行 vs 打转的每步收益差从 ~0.05 拉大到 ~0.18。
  正对墙刹停后的原地转向不受影响（front 有回波时 wobble 门控为 0）。
- **全盲出生课程**：找墙回合中非 head_on 的 40% 改为 d0∈[0.20,0.60]、
  yaw∈[-π,π] 的全盲出生（家居出生分布）；朝向背墙的回合由 ACQUIRE_GRACE
  超时截断，学到"全盲就直走"即达标。
- **墙端 U 型包边课程**（U_TURN_PROB=0.25，frac>0.25 开启）：段间过渡新增
  第三种 —— 下一段贴同一堵墙背面反向延伸、端面对齐，机器人需绕墙端连做
  两个左外角再续贴边，对应门洞侧柱拐入动作。
- **缺口上限压缩**：gap_max 0.20+0.35·frac(→0.55m) 改为 0.20+0.15·frac
  (→0.35m)，与门洞宽度（0.6~0.9m）拉开差距，门洞判为"外角拐入"而非桥接。

### 涉及文件

| 文件 | 改动 |
|---|---|
| `tasks/wall_follow_task.py` | seek 奖励分支、全盲出生、U 型包边过渡、gap 上限 |

### 验证

- 冒烟测试：各课程阶段（iter 0/300/800/1500/3000）reset+随机步无异常、
  奖励有限；200 条高课程随机墙中 44 条含 U 型包边过渡；缺口宽度全部 ≤0.35m；
  家居场景 reset+步进正常；seek 状态注入 yaw_rate=1.5rad/s 时 wobble=-0.060/步
  （防打转罚生效）。
- 行为验证需重训后在家居场景回放确认（出生直行找墙、门洞拐入里屋）。

### 兼容性影响

观测维度、动作空间、XML 均未变；只改任务奖励与课程，旧模型可加载但需重训
才能获得新行为。

---

## [2026-07-09] 家居场景撞墙再修：approach 改为"距墙 1cm 限速"

### 背景 / 现象

昨天已加了 `approach` 前向安全项 + head_on 出生课程，但重训后的策略在家居场景
里**仍然先直冲撞墙、再开始沿墙**（碰一下才停）。目标行为应是：找墙接近时把
速度降到 0、**停在墙前 ~1cm 不接触**，再转向对齐、正常沿墙。

### 根因：旧 approach 是"近墙就罚前进速度"，没有编码"停在哪"

旧式 `approach = confidence × prox × max(fwd_vel,0)/V_DES`（`prox` 在 8cm 内
线性到 1）。问题：

- 它罚的是"近墙处的**全部**前向速度"，连"慢速小心接近"也一并罚 —— 尺度含糊，
  策略学到的是"笼统地慢一点"，而不是"到 1cm 处速度恰好归零"；
- 权重 0.18 偏弱、起罚距离 8cm 偏晚；蹭墙要**持续 1.5s** 才终止，轻轻碰一下
  几乎零代价 —— 于是策略容忍"碰一下再沿墙"，而不是"零接触停住"。

### 修复：把 approach 写成"由前向距离决定的前向限速"

`v_cap(d) = V_DES × clip((d − FRONT_STOP_GAP)/(FRONT_SAFE_DIST − FRONT_STOP_GAP), 0, 1)`
—— 正前方障碍在 **1cm(FRONT_STOP_GAP)** 处允许前向速度 0、在 **9cm(FRONT_SAFE_DIST)**
及以外允许满速 V_DES，中间线性。只惩罚"超过限速的部分"：
`approach = confidence × max(fwd_vel − v_cap, 0)/V_DES`。

- **直接编码"距墙 1cm 停住"**：梯度指向"贴到 1cm 前把速度降到 0"，而不是笼统减速；
- **不误伤**慢速接近 / 内角减速（都在限速内，罚 0），唯有"冲着墙加速"才吃罚；
- 置信度加权：矮墙只挡部分射线、看不真切时罚得轻；
- 权重 0.18 → **0.22**（增强刹车权威）；
- **head_on 课程加强**：占找墙回合比例 45% → 50%；`d0` 改为 **8~35cm**
  （8~10cm 出生的回合一睁眼就看到墙、必须立刻刹车 —— 最强刹车梯度；更远的
  先巡航、进 10cm 量程再刹）；出生偏航放宽到 +90°±(−0.6,+0.5)。

### 涉及文件

| 文件 | 改动 |
|---|---|
| `tasks/wall_follow_task.py` | 新增 `FRONT_STOP_GAP`、`FRONT_SAFE_DIST` 0.08→0.09；`approach` 重写为限速式 overspeed，权重 0.18→0.22；head_on 课程加强 |

### 验证

1. **限速公式**（7 个用例）：9cm 满速→罚 0；5cm 满速冲→罚 0.5、5cm 限速内→0；
   1cm 还在动→重罚、1cm 已停→0；3cm 慢速 0.02→0（限速内）。逐条符合预期。
2. **真实 env 对照**（同一 head_on 出生点朝墙前进）：全力直冲 → 最小前向距离
   5mm、**撞墙**、approach 累积罚 −0.60；按距离自适应刹车 → **不撞墙**、
   approach 累积罚 −0.27。奖励确实在教"距墙减速停住"。
3. **端到端**：2 迭代训练冒烟通过（回报 15.2→9.95，KL 0.005~0.015 健康）。

### 兼容性影响

- **观测/动作维度不变（34/2），奖励 dict 键不变** —— PPO / 回放脚本无需改动；
- 奖励函数已改，**需重训**才能修好"撞墙"行为。与本日"缺口桥接"改动是同一轮
  重训（两处都在 `wall_follow_task.py`）。

---

## [2026-07-09] 墙体缺口（门洞/断墙）桥接：过缺口不再丢墙重搜

### 背景 / 现象

沿边行进时，前方出现一段**两端有墙、中间无墙、地面贯通**的缺口（门洞/断墙）。
机器人抵达缺口、左侧激光丢墙那一刻，策略把它误当"外角/彻底丢墙"处理：
中途减速、原地转向、重新找墙，**连续沿边作业被打断**，不再沿原墙轮廓续行。

### 根因：训练课程里从来没有"缺口"，奖励把"缺口"和"丢墙"混为一谈

- 课程 `_build_random_course` 只会摆**连续折线墙**（段与段之间必是拐角），
  从没出现过"墙—缺口—共线续墙"，策略没学过"直行桥接"这一行为；
- 奖励在丢墙后只有两条分支：`lost_time<1.2s` 的**外角包边宽限**（直行）、
  之后的**真找墙**（`progress*=0.2` 弱化前进 + 吃满丢墙罚）。缺口只要比
  `V_DES×1.2s≈18cm` 宽，跨越途中就落进"真找墙"分支 —— 奖励**主动**把机器人
  从"匀速直行"推向"减速+转向去重新找墙"，这正是观察到的坏行为的直接来源。

### 修复：课程加缺口 + 奖励用真值区分"缺口 vs 外角"

**1) 课程（`_build_random_course`）**：热身后（frac>0.15）在**共线段边界**以
`GAP_PROB=0.35` 概率插入缺口，保持墙向不变、跨过 `GAP_MIN~gap_max` 的无墙段
再续共线墙；`gap_max` 随课程从 0.20m 升到 0.55m。缺口两端墙共线（续墙不做
拐角处的 10cm 回收，避免吃掉缺口），真值记进 `self.gaps=[(近端墙角, 墙向, 宽)]`。

**2) 奖励（`calc_reward`）新增缺口桥接分支**（在外角宽限之前）：用真值
`_gap_bridging()` 判断"是否正沿缺口直行、且前方有共线墙可续贴"：
- 满足 → `progress*=1.0`（全速直行桥接）、`lost*=0.15`（几乎不罚丢墙），
  并把 `wobble` 门控开满（`steady=1`）压住偏航 —— 逼机器人**直行穿过**、
  别拐进缺口；到对侧 `left.hit` 自然回到贴墙分支；
- 不满足（外角处前方**没有**共线墙）→ 走原来的外角包边逻辑，**不受影响**。

**3) 终止（`done`）**：正在桥接缺口时不按"丢墙超时"终止（宽缺口跨越耗时可能
逼近 `LOST_TERM_TIME=4s`），避免中途误杀 episode。

### 关键取舍：缺口宽度上限为何保守（0.55m）

策略是**纯反应式**（只有 2 个短程激光 + 0.2s 历史）。在丢墙那一刻，"缺口"与
"外角"的激光观测**完全一致**（都是左侧空、前方畅通），二者仅在"继续直行后墙
是否在对侧重现"上不同——这是个时序差异，反应式策略无法瞬时区分。因此奖励用
真值区分只是**把正确动作教给策略**，策略最终学到的是"丢墙后直行 D* 距离再考虑
左勾"的折中，D* 由课程里缺口/外角的比例与宽度分布决定。缺口越宽，就越要求策略
直行更久，代价是外角处越容易冲过头。上限取 0.55m 是为了**桥接中小缺口、又不
明显回退昨天刚修好的外角包边（阳角卡角）**。>0.55m 的宽门洞暂列为已知局限。

### 涉及文件

| 文件 | 改动 |
|---|---|
| `tasks/wall_follow_task.py` | 新增缺口常量 / `self.gaps`/`self.bridging`；`_gap_bridging()` 真值判定；`step()` 每步缓存 `bridging`；`calc_reward` 缺口桥接分支 + `wobble` 门控扩展；`done` 桥接时不按丢墙终止；`_build_random_course` 插入共线缺口 |

### 验证

1. **`_gap_bridging` 纯逻辑**（8 个用例全过）：缺口内朝墙向直行→True；
   缺口内朝墙外拐 90°/横向跑偏 0.5m/未到/已过/反向→False；空 gaps→False。
2. **课程真的摆出缺口**：40 次 reset（frac=1.0）中 12 次生成缺口，两端墙共线
   （墙向一致）、宽度落在课程区间。
3. **奖励分支生效**：把机器人放进缺口中点朝墙向→`bridging=True`，
   `reward.lost=-0.0075`（桥接分支 = -0.05×0.15），非桥接丢墙为 -0.05。
4. **环境无异常**：300 步随机动作 step/reward/done 跑通，reward 均有限。
5. **端到端**：2 迭代训练冒烟通过（4 进程并发、回报 8.7、KL≈0.007 健康）。

### 兼容性影响

- **观测/动作维度不变（34/2），奖励 dict 键不变** —— PPO / 回放脚本无需改动；
- 奖励函数已改，**需重训**才能让策略学会桥接缺口（旧模型仍可加载回放，
  但不会桥接）；家居场景回放 `gaps=[]`，`bridging` 恒 False，回放行为由重训后
  的策略决定。
- 规则控制器 `wall_follower.py` **未改**：其 `WRAP_LEFT` 在宽缺口处同样会误当
  外角左勾（已知局限，本次未涉及）。

---

## [2026-07-08] 重训后三个行为缺陷修复：阳角卡角 / 沿墙摇晃 / 家居场景撞墙

### 背景 / 动机

幽灵墙修复后重训的模型，仿真中暴露三个行为缺陷：

1. **阳角（外角）转弯时墙角卡进机身**；
2. **沿墙行进时机身一直摇晃**；
3. **家居场景里先直冲撞墙、再开始正常沿墙**。

逐个定位后发现：#2 的主因是一个**一直存在的代码 bug**（平滑惩罚恒为 0），
#1 和 #3 则是奖励/课程的设计缺口（分别缺"包边宽限"和"前向减速"）。

### 根因 1（bug）：平滑惩罚 `smooth` 恒等于 0 —— 沿墙摇晃

- `envs/vacuum/robot.py` 的 `step()` 在 `do_simulation` 之后把 `prev_action`
  覆盖成**本步**动作；而 `env.step()` 的顺序是先 `robot.step(a)` 再
  `calc_reward(..., self.robot.prev_action, a)`。到达奖励函数时
  `prev_action` 已经等于 `a`，于是 `dact = a - a ≡ 0`。
- **后果**：`smooth` 项自始至终是 0，动作变化率从未被惩罚 —— 策略可以任意
  高频抖动而零代价，机身自然一直摇晃。`vacuum` 过坎任务走同一条代码路径，
  同样中招。
- **修复**：`VacuumRobot` 新增 `last_action / last_torque`，在覆盖 `prev_*`
  之前抓取真正的上一步值；两个 env 的 `calc_reward` 改传 `last_action`。
  `prev_torque` 保持不变传给能耗项（它本就该是"本步实际施加的力矩"）。
  观测里的 `prev_action` 也保持不变（观测在 `robot.step` 之后取，
  "最近一次动作"正是策略该看到的量）。

### 根因 2：奖励里没有"接近障碍必须减速"项 —— 家居场景撞墙

- 前向激光虽然在观测里（34 维中占 2 维 + 历史帧），但**奖励从不依赖它**，
  策略没有任何理由学会用它刹车。训练里墙又永远在左侧、从不正面出现，
  于是"正对墙面"这个状态在训练分布里几乎不存在（OOD）。
- **修复（两处，缺一不可）**：
  - 奖励新增 `approach` 项：正前方 8cm 内有障碍时，
    `罚 ∝ 置信度 × 接近程度 × max(前向速度, 0)` —— 只在真的朝障碍前进时生效；
  - 课程新增 **head_on 出生模式**（占找墙回合的 45%）：机器人正对墙面
    （yaw≈+90°，离墙 12~30cm）出生，必须减速并转向对齐才能沿墙。

### 根因 3：阳角处策略"急着左勾"把墙角勾进机身

- 左侧激光装在机体**横向中心线**（x=0）上。丢墙那一刻，墙角恰好在机身正侧方；
  此时若立刻左转，机身左前缘会扫向仍在原地的墙角 → 卡角。
- 旧奖励在丢墙时 `progress *= 0.2` 且吃满 `lost` 罚，等于**逼**策略尽快左勾找墙。
- **修复**：识别阳角信号（左墙刚丢 `lost_time < 1.2s` **且**前方无障碍），
  在该窗口内 `progress *= 0.6`、`lost *= 0.3` —— 鼓励先直行越过墙角再左转。
  窗口 1.2s 由几何定出：把墙角甩到机身后方需再前进约一个底盘半径
  （`0.175m / V_DES 0.15 ≈ 1.17s`）。
  该策略与规则控制器 `WRAP_LEFT`（先直行 20cm 再画圆弧）的做法一致。
- 退化解检查：窗口内"贴着量程边缘吊着走"最多拿 `0.26×0.6 − 0.05×0.3 ≈ 0.14/步`，
  远低于正常贴墙跟踪（`track+progress` 可达 ~0.45/步），不构成新的局部最优。

### 关键取舍：抖动抑制项该罚什么？（实测否定了第一版设计）

先试了罚**偏航角加速度**（"正常拐弯是恒角速度圆弧、摇晃是角速度反复反号"）。
实测直接否掉：规则控制器的 `|yaw_accel|` 均值已达 **8.6 rad/s²**（被接触/求解
噪声淹没），注入振荡后只涨到 9.7（**1.1x，无判别力**）；`rms|dact|` 同样只有
1.0x。**唯一有判别力的是直行门控下的 `|yaw_rate|`**（注入振荡 1.5~2.0x，且随
振幅单调）。原设计的信号是对的，错在尺度：`(yaw_rate/2.0)²` 让规则控制器只被
罚 -0.0006，比 track 收益小 200 倍，等于没有。

于是在实测分布上标定 `wobble = track × min((|yaw_rate| / S)², 1)`：

| S | rule | wob0.3 | wob0.5 | wob0.8 | wob0.8/rule |
|---|---|---|---|---|---|
| 0.4 | -0.0147 | -0.0193 | -0.0244 | -0.0294 | 2.00x |
| **0.6** | **-0.0080** | **-0.0112** | **-0.0157** | **-0.0222** | **2.78x** |
| 0.8 | -0.0048 | -0.0067 | -0.0099 | -0.0155 | 3.20x |
| 2.0（原） | -0.0010 | -0.0012 | -0.0017 | -0.0028 | 2.90x |

取 **S=0.6, W=0.10**：判别比 2.78x，绝对量级够形成梯度，规则控制器只被罚掉
track 收益的 7%；且远低于丢墙罚 0.05/步 —— 策略不会为躲这项而放弃贴墙
（`wobble` 被 `track` 门控，不贴墙反而损失更大）。

### 奖励权重调整

| 项 | 旧 | 新 | 说明 |
|---|---|---|---|
| track | 0.30 | 0.28 | 让出权重给新项 |
| progress | 0.30 | 0.26 | 同上 |
| scrape | 0.20 | 0.20 | 不变 |
| `approach` | — | **0.18** | 新增：接近障碍必须减速 |
| `wobble` | — | **0.10** | 新增：直行段抖动抑制 |
| smooth | 0.04 | 0.06 | 首次真正生效（此前恒为 0） |
| upright / lost / energy | 0.05 / 0.05 / 0.04 | 不变 | |

### 涉及文件

| 文件 | 改动 |
|---|---|
| `envs/vacuum/robot.py` | 新增 `last_action/last_torque`（覆盖 `prev_*` 前抓取），修复平滑惩罚恒为 0 |
| `envs/vacuum/vacuum_wf_env.py` | `calc_reward` 改传 `last_action`；reset 复位 `last_*` |
| `envs/vacuum/vacuum_env.py` | 同上（过坎任务同一 bug） |
| `tasks/wall_follow_task.py` | 新增 `approach`/`wobble` 项、阳角包边窗口、head_on 出生课程；`step()` 采集 `yaw_rate`；权重重平衡 |

### 验证

1. **行为排序**（6 回合 × 400 步，随机墙课程）：

   | 控制器 | 总分/步 | track | scrape | approach | wobble | smooth | 回合长 |
   |---|---|---|---|---|---|---|---|
   | 规则控制器 | **+0.2049** | +0.1190 | -0.0153 | -0.0003 | -0.0080 | -0.0124 | 跑满 400 |
   | 撞墙（满力矩直冲） | -0.0648 | +0.0167 | -0.1018 | **-0.0164** | -0.0007 | 0 | 67~170 步被终止 |
   | 原地打转 | -0.0102 | +0.0015 | 0 | -0.0001 | -0.0006 | 0 | 跑满 |

   规则控制器（已知正解）**没有被新罚项误伤**：`approach=-0.0003`、
   `wobble` 只占 track 收益 7%；撞墙策略蹭墙步占比 50.9%、被提前终止。
2. **抖动判别力**：同一规则控制器叠加 2.5Hz 差动振荡后，`wobble` 从 -0.0080
   → -0.0157（**1.97x**）。（不比较总分：振荡版恰好少蹭墙，不是"严格更差"的
   控制器，总分排序对它没有意义。）
3. **平滑项回归守卫**：断言 `smooth ≠ 0`；`vacuum` 过坎任务在变化动作下
   `smooth` 均值 -0.0022（修复前恒为 0）。
4. **前向安全项**：正对 3cm 墙面满速前进 → `approach=-0.111`；前方无障碍 → 恰为 0。
5. **head_on 课程**：60 次 reset 采到 9 次，其中 **9/9** 前向激光确实能捕获墙面。
6. **端到端**：3 迭代训练冒烟通过（回报 10.9→23.9，KL≈0.017 健康）；
   `vacuum` 过坎环境观测仍为 23 维、正常运行。

### 兼容性影响

- **观测维度不变（`vacuum_wf` 34 维 / `vacuum` 23 维），动作维度不变** ——
  旧模型仍可加载回放，但**奖励函数已改，需要重训才能修好这三个行为**；
- 奖励 dict 新增 `approach`/`wobble` 两个键。PPO 只用标量
  `sum(rewards.values())`，回放脚本按 `.keys()` 泛化打印，均无需改动；
- `vacuum` 过坎任务的 `smooth` 项由"恒为 0"变为真实生效，其奖励尺度有变化，
  **旧的过坎训练结果需重训**（该任务的旧结果本就因幽灵墙物理而无效）；
- 规则控制器 demo（`wall_follow_demo.py`）不受影响。

---

## [2026-07-07] eval.svg 横轴刻度优化

- **现象**：评估曲线横轴按 `eval_freq`(50) 打刻度，1500 迭代时 30 个刻度挤在
  一起看不清。
- **修复**：`rl/algos/ppo.py` 绘图刻度固定 200 间隔；并用同样式重渲染了
  `experiments/vacuum_wf/eval.svg`。以后所有训练自动生效。

---

## [2026-07-07] 回放脚本加观测维度校验（修复"闪退"）

- **现象**：`debug_wallfollow.py --path experiments/vacuum_wf` 启动即崩溃。
- **原因**：加载的是幽灵墙修复前训练的旧模型（观测 22 维），环境已升级为
  34 维，输入层维度不匹配直接抛异常退出。
- **修复**：`scripts/debug_wallfollow.py` 在运行前比对 `policy.obs_mean` 维度
  与环境观测维度，不匹配时打印中文说明（原因+解决方法）后正常退出。
- **提醒**：旧 `vacuum_wf` 模型本身无效（在无碰撞幽灵墙上训练），
  必须用修复后重训的模型回放。

---

## [2026-07-07] 沿边 RL 训练失败根因修复：幽灵墙 / 奖励漏洞 / 泛化缺陷

### 背景

`vacuum_wf` 首轮 1500 迭代训练后效果极差：随机墙场景中机器人撞角后**整体嵌入墙体**；
家居泛化场景中**原地打转**，完全没有沿边行为。排查后定位到一个物理层 bug（主因）
和多个奖励/课程设计缺陷（次因）。

### 根因 1（主因）：运行时摆放的墙是"幽灵"，物理上可穿透

- **现象**：最小隔离实验证实，MuJoCo 2.3.6 在**编译期**把世界静态 geom 的
  位置/尺寸烘焙进碰撞加速结构，运行时修改 `model.geom_pos / geom_size`
  **对碰撞检测完全不生效**（激光 `mj_ray` 却读实时位置，"看得见、撞不着"）。
- **后果**：训练时所有随机墙没有物理实体，策略学到"穿墙走"；到家居场景
  （XML 原生墙、碰撞真实）立即崩溃。**原 `vacuum` 过坎任务的门槛（box01 运行时
  摆放）也是同样的幽灵，此前该任务的物理同样是坏的。**
- **修复**：所有动态地形改为 **mocap body**（MuJoCo 运行时摆放物体的标准机制，
  碰撞跟随 `data.mocap_pos/quat`）。约束：geom 尺寸必须固定 → 采用固定尺寸组合：
  - 沿边训练墙 `box01~06`：半长 0.80/0.60/0.45 各两块，半厚 0.08、半高 0.125；
  - 过坎门槛 `ridge01`(0.06,1.00,0.05)、AGAINST 短墙 `ridge02`(0.06,0.30,0.05)、
    骑坎窄脊 `ridge03`(1.00,0.05,0.05)；
  - **不同有效墙高用"下沉入地"实现**（mocap body 与 world 同属一个 weld，
    与地面不产生接触，可以任意下沉）。

### 根因 2：奖励函数存在"压墙滑行"局部最优

- **现象**：斜压在墙上滑行可白拿满速度分（progress 只看前向速度），蹭墙惩罚
  0.10 太轻，净收益为正 → 策略收敛到贴墙硬推。
- **修复**（`tasks/wall_follow_task.py`）：
  - progress 用贴边质量门控：`progress *= (0.25 + 0.75*track)`，贴不准拿不到速度分；
  - 蹭墙惩罚 0.10 → 0.20，且**机身持续接触 1.5s 直接终止 episode**；
  - 修复后实测：压墙滑行 = **-0.119/步且 75 步被终止**（修复前 +0.075 跑满全程）。

### 根因 3：策略从没学过"找墙" → 家居场景出生点观测出分布(OOD)

- **修复**：35% 回合出生在离墙 12~45cm 处（找墙课程），首次捕获墙面前丢墙宽限
  8s（捕获后 4s）。策略必须学会"先接近、捕获墙面，再贴边"。

### 根因 4：拐角处部分可观测，前馈网络单帧激光分不清"刚丢墙"与"一直没墙"

- **修复**：观测追加激光历史帧（前向/左侧的距离+置信度在 0.05/0.1/0.2s 前的
  快照，共 12 维），**观测 22 → 34 维**。

### 附带修复：HIGHCENTER（骑坎）场景几何错误

- 原设计凸脊沿 y 摆放，实际会先托住中线上的前后万向球 → 轮子完全悬空，
  是不可自救的死局（此前被幽灵物理掩盖，从未真实生效）。
- 改为 `ridge03` 窄脊（宽 10cm）沿行进方向、横向偏置 ±8cm，从万向球
  (|y|<0.022) 与驱动轮 (|y|>0.138) 之间的空隙下方穿过：腹部压脊、机身微倾、
  单侧轮打滑但另一侧仍有抓地 —— 可脱困，符合"骑坎打滑"的设计意图。

### 其他加固

- 训练墙/门槛接触加硬（`geom_solref=(0.005,1)`, `solimp=(0.95,0.99,0.001)`），
  防止大力矩顶墙深度穿透（`envs/vacuum/vacuum_wf_env.py`）。

### 涉及文件

| 文件 | 改动 |
|---|---|
| `envs/vacuum/gen_xml.py` | box 由静态 geom 改为 mocap body；新增 `WALL_BOX_SPECS`/`RIDGE_BOX_SPECS` 固定尺寸组合 |
| `tasks/wall_follow_task.py` | mocap 摆墙 + 下沉调高；奖励门控与蹭墙终止；找墙课程；`_bury_all_boxes` 遍历 mocap body |
| `tasks/vacuum_task.py` | 门槛/短墙/骑坎脊改用 mocap（ridge01/02/03）；HIGHCENTER 几何重设计 |
| `envs/vacuum/vacuum_wf_env.py` | 激光历史帧观测(34 维)；地形接触加硬 |
| `scripts/test_line_laser.py` | 适配 mocap 摆放；7cm 边界用例预期修正、新增 8cm 用例 |

### 验证

1. 最小隔离实验：静态 geom 运行时改 pos/size → 无碰撞；mocap body → 碰撞正确
   （含旋转、下沉、埋地、动力学承重）；
2. 奖励排序：规则控制器 +0.456/步 ≫ 压墙 -0.119（被终止）≫ 打转 +0.001；
3. 全力矩正面顶墙 8s：机身被真实挡住（修复前直接穿墙飞出 6m）；
4. 激光特性 11 项断言全过；CROSS 门槛/HIGHCENTER 骑脊接触真实；
5. 12 迭代试训练：评估回报 51.6 → 86.7 稳步上升，KL≈0.006 健康。

### 兼容性影响（重要）

- **观测维度 22→34：`vacuum_wf` 旧模型全部作废，必须从头重训**；
- `vacuum` 过坎任务此前的物理是坏的，**旧的过坎训练结果无效，需要重训**；
- 规则控制器 demo（`wall_follow_demo.py`）不受影响（家居墙为 XML 原生）。

---

## [2026-07-07] PPO 接入沿边任务 + 算法优化 + 边刷旋转

### 新增

- **`vacuum_wf` 环境**（`envs/vacuum/vacuum_wf_env.py`，注册于 `run_experiment.py`）：
  沿边沿墙 RL 训练环境。观测只含激光 + 本体感知（无全局坐标/特权信息），
  这是策略能泛化到任意户型的关键；`home_scene=True` 变体用于泛化回放。
- **`WallFollowTask`**（`tasks/wall_follow_task.py`）：随机折线墙课程（段数/拐角
  角度方向/墙高随迭代展开）、激光噪声课程(0→2mm)、地面摩擦随机化；
  奖励 = 贴边(1cm) + 沿墙推进 + 水平 − 丢墙 − 蹭墙 − 能耗 − 平滑。
- **回放脚本** `scripts/debug_wallfollow.py`（`--home` 家居泛化验证）。

### PPO 算法优化（`rl/algos/ppo.py`）

- **实现真 GAE(λ)**：原 `use_gae` 参数形同虚设（优势=MC回报−V，方差大）。
  现采样时反向扫描计算 GAE 优势，critic 目标改用 TD(λ) 回报；
- **KL 早停可用**：`target_kl` 原写死 None，现由 `--target_kl` 传入（建议 0.04）；
- **`--lr_decay`**：学习率随迭代线性衰减（至 5% 下限）。
- 均向后兼容：不传新参数时行为与原版一致。

### 边刷旋转（`envs/vacuum/gen_xml.py` + `envs/vacuum/robot.py`）

- 左右三叶边刷改为挂在底盘上的独立 body + 绕 z 铰链关节，`VacuumRobot`
  每个仿真子步以 12 rad/s 运动学驱动（左刷顺时针/右刷逆时针，向机身中线扫拢）。
  不占执行器，不改动作/观测维度；旧 `vacuum` 环境观测仍为 23 维。

### 验证

- 边刷转向/转速正确；`vacuum` 环境兼容冒烟通过；
- 3 迭代端到端训练冒烟通过（ray 12 进程 XML 按 pid 隔离写入，无并发冲突）。

---

## [2026-07-06] 沿边沿墙功能落地：线激光建模 + 规则沿墙控制器

### 新增

- **线激光传感器 MuJoCo 建模**（`envs/vacuum/gen_xml.py`）：
  前向/左侧各一个，每个用同一位置、离地 3.5/4.7/5.8/7.0cm 的 4 条 rangefinder
  射线近似竖直激光条纹；`cutoff=0.10` 体现 10cm 量程。特性由几何自然呈现：
  ≥7cm 全命中（可靠）、4~6cm 部分命中（低置信）、2~3cm 低于最低射线（盲区）。
- **传感器读取封装**（`envs/vacuum/sensors.py`）：`LineLaser.read()` →
  `distance / confidence / hit`，支持测距噪声；注意 cutoff 会把超量程命中钳到
  恰好 0.10，判定必须用严格小于。
- **规则沿墙控制器**（`envs/vacuum/wall_follower.py`）：
  状态机（SEEK 找墙 / TURN_RIGHT 内角 / FOLLOW 贴边 / WRAP_LEFT 外角包边 /
  ESCAPE 盲区脱困）+ 横向距离 PD（KP=8/KD=16，按闭环 `e''+vKD·e'+vKP·e=0`
  临界阻尼整定）+ 差速运动学 + 轮速 PI → 轮力矩。
  关键细节：右转对齐以"左侧距离越过最小值回升"判定；盲区障碍（毯沿等激光
  看不见）用里程计失速检测触发倒车换向脱困。
- **演示/验证脚本**：`scripts/wall_follow_demo.py`（激光射线可视化 + 贴边统计）、
  `scripts/test_line_laser.py`（量程/检测高度特性断言）。

### 场景调整

- 地毯从北墙沿边路径挪到外间东南空地并降为 12mm：原 16mm > 15mm 底盘间隙
  且在激光盲区，正好压在沿边环线上会把底盘卡死；
- 环境暴露 `env.laser_front / env.laser_left`，RL 观测保持 23 维不变。

### 验证

- 传感器特性断言全过（测距 3/6/9cm 准确、12cm 无回波、2/3cm 盲区、5cm 低置信、
  ≥7cm 可靠）；
- 300s 无渲染仿真：完整巡边环线可重复（北墙→沙发轮廓→隔断→门洞鼻端包边→
  南墙→木柜轮廓→西墙→闭环），稳态贴边误差中位 1.4mm、直线段稳定 10mm，
  盲区障碍（充电座底板）触发脱困后正常继续。

---

## 记录模板（新增记录时复制使用）

```markdown
## [日期] 标题（一句话概括）

### 背景 / 动机
为什么要改（现象、需求、缺陷表现）。

### 改动内容
改了什么、为什么这样改（关键设计取舍写清楚）。

### 涉及文件
| 文件 | 改动 |
|---|---|

### 验证
怎么证明改对了（测试脚本、实测数据）。

### 兼容性影响
旧模型/旧接口/其他任务是否受影响。
```
