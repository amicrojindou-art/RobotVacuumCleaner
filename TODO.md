# 工作清单

## 今天已完成（2026-07-07）

1. **定位并修复"幽灵墙"根本 bug**：MuJoCo 运行时摆放的静态地形无碰撞（可穿透），
   全部动态地形改为 mocap body（固定尺寸组合 + 下沉调高），连带修复过坎任务
   （CROSS/HIGHCENTER/AGAINST）的物理有效性。
2. **奖励与训练体系优化并完成重训**：贴边质量门控速度分、蹭墙重罚+持续接触终止、
   找墙课程（35% 远离墙出生）、激光历史帧观测（22→34 维）；1500 迭代重训完成，
   评估回报收敛、回合长贴顶。
3. **工具链完善**：回放脚本加观测维度校验（修复闪退）、eval.svg 横轴刻度改 200
   间隔、建立 CHANGELOG.md 修改记录表。

## 今天已完成（2026-07-08）

三个行为缺陷已定位并修复（详见 CHANGELOG）：

1. **阳角卡角** → 识别外角信号（刚丢墙 + 前方无障碍），给 1.2s 包边宽限窗口，
   窗口内鼓励先直行越过墙角再左转（1.2s 由"甩开墙角需前进一个底盘半径"定出）。
2. **沿墙摇晃** → 定位到**真 bug**：`prev_action` 在 `calc_reward` 前已被覆盖成
   本步动作，`smooth` 平滑惩罚自始至终恒为 0（`vacuum` 过坎任务同样中招）。
   修复后再加 `wobble` 偏航角速度抑制项，尺度在实测分布上标定（S=0.6, W=0.10）。
   *（弯路记录：先试的"罚偏航角加速度"被实测否掉 —— 被接触噪声淹没，判别力仅 1.1x。）*
3. **家居撞墙** → 奖励新增 `approach` 前向安全项（接近障碍必须减速）+ 课程新增
   head_on 正对墙面出生模式。二者缺一不可：前者让前向激光在奖励里"有用"，
   后者让"正对墙面"这个状态进入训练分布。

已通过：行为排序（规则控制器未被新罚项误伤）、抖动判别力 1.97x、
平滑项回归守卫、head_on 几何 9/9、3 迭代端到端冒烟、`vacuum` 环境兼容。

## 明天计划（2026-07-09）

1. **重训 `vacuum_wf`**（观测/动作维度未变，但奖励已改，必须重训）：
   `--n_itr 1500 --target_kl 0.04 --lr_decay`，输出到新目录（如 `experiments/vacuum_wf3`）。
2. **按量化标准验收**（而不是只看回报曲线）：
   - [ ] 阳角：回放统计包边过程中的 `scrape` 步数应≈0；
   - [ ] 摇晃：直行段 `|yaw_rate|` 均值应 < 0.21（规则控制器水平）；
   - [ ] 撞墙：家居场景 `--home` 回放，首次接触墙前 `approach` 罚应≈0（说明提前减速）。
3. **重训 `vacuum` 过坎任务**：其 `smooth` 项由恒 0 变为生效，奖励尺度已变。
